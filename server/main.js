import { Meteor } from 'meteor/meteor';

import '../imports/api/pages.js';
import '../imports/api/stls.js';
import '../imports/api/hackers.js';

Meteor.startup(() => {
  // code to run on server at startup
});
